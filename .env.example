# Personal overrides for local development. Copy to .env and adjust as needed.
# This file is not loaded automatically; Makefile reads .env.development first,
# then .env for machine-specific settings.

# Force Docker mode to use the local WSL socket and override any inherited DOCKER_HOST.
CLAWORC_DOCKER_HOST=unix:///var/run/docker.sock
